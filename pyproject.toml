[project]
name = "homeos-purchase-tracker-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cesar Martinez Razola", email = "cmartinezrazola@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
    "django-filter>=25.2",
    "djangorestframework>=3.17.1",
    "gunicorn>=26.0.0",
    "markdown>=3.10.2",
    "psycopg[binary]>=3.3.4",
]

[tool.uv]
managed = true
package = false

[dependency-groups]
dev = [
    "mkdocs-material>=9.7.6",
    "pytest>=9.0.3",
]
